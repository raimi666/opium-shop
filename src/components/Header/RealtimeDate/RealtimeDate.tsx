import React from 'react';
import {Clock} from "./Clock/Clock.tsx";

type RealtimeDateProps = {

};

export const RealtimeDate = ({}:RealtimeDateProps) => {
    return (
        <div>
            <Clock/>
        </div>
    );
};
